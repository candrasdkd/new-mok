import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bodyContainer: {
        alignItems: 'center',
        justifyContent: 'center',
        // flex: 1, // Tambahkan flex: 1 jika perlu
    },
});
